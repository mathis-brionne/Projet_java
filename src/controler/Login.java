package controler;

import model.Utilisateur;
import model.bdd_find;
/*
public class Login {
    private Utilisateur user = null ;

    public Login(String username , String passwords) {
        try {
            Utilisateur a = new bdd_find().find(username);
            System.out.println(passwords + a.getPassword());
            if (a.getPassword().equals(passwords)) {
                view.Home_Page h = new view.Home_Page();
            } else {
                view.Login L = new view.Login();
            }
        } catch (ClassNotFoundException c) {
            c.getException();
        }
    }
}
*/